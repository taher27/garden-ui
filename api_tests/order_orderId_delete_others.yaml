# Test generated by RoostGPT for test testArtilleryEvent0611 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=6e4c4bbafe

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "order_orderId_delete_others.csv"
    fields: ["orderId"]
    skipHeader: true
scenarios:
  - name: "Delete order API test"
    flow:
      - log: "Invalid ID supplied test"
        post:
          url: "/store/order/{{ orderId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - equals: 
                description: "Invalid ID supplied"
        think: 1
      - log: "Order not found test"
        post:
          url: "/store/order/{{ orderId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "description"
            - equals: 
                description: "Order not found"
        think: 1
      - log: "Invalid URL test"
        post:
          url: "/store/orders/{{ orderId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1
      - log: "Authorization fail test"
        post:
          url: "/store/order/{{ orderId }}"
          headers:
            Authorization: "invalid_token"
          expect:
            - statusCode: 401
        think: 1

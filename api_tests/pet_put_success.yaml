# Test generated by RoostGPT for test test_API_SPEC_Postman0711 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=831e53a895

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "pet_put_success.csv"
    fields:
      - "id"
      - "name"
      - "category"
      - "photoUrls"
      - "tags"
      - "status"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Update an existing pet"
    flow:
      - post:
          url: "/pet"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            category: "{{ category }}"
            photoUrls: "{{ photoUrls }}"
            tags: "{{ tags }}"
            status: "{{ status }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "category"
            - hasProperty: "photoUrls"
            - hasProperty: "tags"
            - hasProperty: "status"
          log: "Update pet request sent"
      - think: 1

# Test generated by RoostGPT for test test_API_SPEC_Postman0711 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=9d403edcba

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "findByTags_get_others.csv"
    skipHeader: true
    fields:
      - "tags"
  defaults:
    headers:
      Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: "Find pet by tags negative scenarios"
    flow:
      - think: 5
      - get:
          url: "/pet/findByTags?tags={{ tags }}"
          headers:
            Authorization: "InvalidToken"
          beforeRequest: "log"
          expect:
            - statusCode: 401
        # Request with invalid token should return 401
      - think: 5
      - get:
          url: "/pet/findByTags?tags=InvalidTag"
          beforeRequest: "log"
          expect:
            - statusCode: 400
        # Request with invalid tag should return 400
      - think: 5
      - get:
          url: "/invalidUrl"
          beforeRequest: "log"
          expect:
            - statusCode: 404
        # Request to invalid URL should return 404
      - think: 5
      - get:
          url: "/pet/findByTags"
          beforeRequest: "log"
          expect:
            - statusCode: 400
            - hasProperty: "tags"
        # Request without tags should return 400 and have 'tags' property in the response

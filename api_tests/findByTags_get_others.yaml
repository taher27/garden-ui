# Test generated by RoostGPT for test testArtilleryEvent0611 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=c43742dac4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "findByTags_get_others.csv"
    fields:
      - "tags"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Test Failure and Edge Cases for /pet/findByTags API"
    flow:
      - log: "Test Case 1: Invalid URL"
        get:
          url: "/invalidUrl"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "content-type"
        think: 1
      - log: "Test Case 2: Unauthorized Access"
        get:
          url: "/pet/findByTags"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasHeader: "content-type"
        think: 1
      - log: "Test Case 3: Invalid Tag Value"
        get:
          url: "/pet/findByTags?tags=invalidTag"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "content-type"
            - body: "Invalid tag value"
        think: 1
      - log: "Test Case 4: No Tags Provided"
        get:
          url: "/pet/findByTags"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            tags: ""
          expect:
            - statusCode: 400
            - hasHeader: "content-type"
            - body: "Invalid tag value"
        think: 1

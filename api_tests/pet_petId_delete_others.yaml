# Test generated by RoostGPT for test testArtilleryEvent0611 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=fbd3e66b3b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_petId_delete_others.csv"
    fields:
      - "petId"
    skipHeader: true
  environments:
    production:
      target: "{{ $processEnvironment.API_HOST }}"
      phases:
        - duration: 10
          arrivalRate: 5

scenarios:
  - name: "Delete Pet API - failure and edge cases"
    flow:
      - think: 5
      - log: "Test Case 1: Delete request with invalid URL"
      - delete:
          url: "/pet/invalidUrl"
          headers:
            api_key: "{{ api_key }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
      - log: "Expected behavior: The API should return a 404 status code with a 'message' property in the response body."
      - think: 5
      - log: "Test Case 2: Delete request with invalid petId (non-integer value)"
      - delete:
          url: "/pet/{nonIntegerPetId}"
          headers:
            api_key: "{{ api_key }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - log: "Expected behavior: The API should return a 400 status code with a 'message' property in the response body."
      - think: 5
      - log: "Test Case 3: Delete request with missing 'Authorization' header"
      - delete:
          url: "/pet/{{ petId }}"
          headers:
            api_key: "{{ api_key }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
      - log: "Expected behavior: The API should return a 401 status code with a 'message' property in the response body."
      - think: 5
      - log: "Test Case 4: Delete request with invalid 'Authorization' token"
      - delete:
          url: "/pet/{{ petId }}"
          headers:
            api_key: "{{ api_key }}"
            Authorization: "InvalidToken"
          expect:
            - statusCode: 403
            - hasProperty: "message"
      - log: "Expected behavior: The API should return a 403 status code with a 'message' property in the response body."

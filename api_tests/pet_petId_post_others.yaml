# Test generated by RoostGPT for test test_API_SPEC_Postman0711 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=05e490ab2a

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_petId_post_others.csv"
    fields:
      - "petId"
      - "name"
      - "status"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Test Scenario"
    flow:
      - log: "Sending request with invalid petId"
        post:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          query:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 405
            - hasProperty: "message"
        think: 1
      - log: "Sending request with invalid name"
        post:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          query:
            name: "invalid_name"
            status: "{{ status }}"
          expect:
            - statusCode: 405
            - hasProperty: "message"
        think: 1
      - log: "Sending request with invalid status"
        post:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          query:
            name: "{{ name }}"
            status: "invalid_status"
          expect:
            - statusCode: 405
            - hasProperty: "message"
        think: 1
      - log: "Sending request with invalid petId, name and status"
        post:
          url: "/pet/invalid_petId"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          query:
            name: "invalid_name"
            status: "invalid_status"
          expect:
            - statusCode: 405
            - hasProperty: "message"
        think: 1
      - log: "Sending request with invalid auth token"
        post:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "invalid_auth_token"
          query:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
        think: 1

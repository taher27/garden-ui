# Test generated by RoostGPT for test testArtilleryEvent0611 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=99d9f02040

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "pet_petId_post_others.csv"
    fields:
      - "petId"
      - "name"
      - "status"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Test Scenario"
    flow:
      - think: 1
      - post:
          url: "/pet/{{ petId }}"
          headers:
            Content-Type: "application/x-www-form-urlencoded"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 405
            - hasProperty: "message"
          log: "Testing API with valid data but expecting 405 status code as only this is mentioned in API spec"
      - think: 1
      - post:
          url: "/pet/{{ petId }}"
          headers:
            Content-Type: "application/x-www-form-urlencoded"
            Authorization: "InvalidToken"
          formData:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 401
          log: "Testing API with invalid authorization token expecting 401 status code"
      - think: 1
      - post:
          url: "/pet/InvalidId"
          headers:
            Content-Type: "application/x-www-form-urlencoded"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 404
          log: "Testing API with invalid petId expecting 404 status code"
      - think: 1
      - post:
          url: "/invalidUrl"
          headers:
            Content-Type: "application/x-www-form-urlencoded"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 404
          log: "Testing API with invalid URL expecting 404 status code"

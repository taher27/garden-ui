# Test generated by RoostGPT for test test_API_SPEC_Postman0711 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=9bff30936b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./processor.js"
  ensure:
    p99: 300
  phases:
    - duration: 120
      arrivalRate: 5
  payload:
    path: "findByStatus_get_others.csv"
    fields:
      - "status"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Find pets by status failure cases"
    flow:
      - log: "Invalid status value"
        get:
          url: "/pet/findByStatus?status=unknown"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - equals: 
                - "{{ message }}"
                - "Invalid status value"
        think: 1
      - log: "Testing with invalid url"
        get:
          url: "/pet/invalidUrl?status=available"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1
      - log: "Testing with invalid authorization token"
        get:
          url: "/pet/findByStatus?status=available"
          headers:
            Authorization: "Bearer invalidToken"
          expect:
            - statusCode: 401
        think: 1
      - log: "Testing with missing authorization header"
        get:
          url: "/pet/findByStatus?status=available"
          expect:
            - statusCode: 401
        think: 1
      - log: "Testing with extra query parameter"
        get:
          url: "/pet/findByStatus?status=available&extraParam=value"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
        think: 1

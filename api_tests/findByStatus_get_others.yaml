# Test generated by RoostGPT for test testArtilleryEvent0611 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=8a6351bbc5

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "findByStatus_get_others.csv"
    fields:
      - "status"
    skipHeader: true
  environments:
    prod:
      target: "{{ $processEnvironment.API_HOST }}"
      variables:
        authToken: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Petstore - Test failure cases for findByStatus endpoint"
    flow:
      - log: "Sending request with invalid status value"
        get:
          url: "/pet/findByStatus?status=invalidStatus"
          headers:
            Authorization: "{{ authToken }}"
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "message"
        think: 5
      - log: "Sending request without any status value"
        get:
          url: "/pet/findByStatus"
          headers:
            Authorization: "{{ authToken }}"
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "message"
        think: 5
      - log: "Sending request with invalid authorization"
        get:
          url: "/pet/findByStatus?status=available"
          headers:
            Authorization: "InvalidToken"
            Content-Type: "application/json"
          expect:
            - statusCode: 401
            - hasProperty: "message"
        think: 5
      - log: "Sending request to invalid URL"
        get:
          url: "/pet/invalidUrl"
          headers:
            Authorization: "{{ authToken }}"
            Content-Type: "application/json"
          expect:
            - statusCode: 404
            - hasProperty: "message"
        think: 5

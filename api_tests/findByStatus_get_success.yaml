# Test generated by RoostGPT for test test_API_SPEC_Postman0711 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=9bff30936b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "findByStatus_get_success.csv"
    fields:
      - "status"
    skipHeader: true
scenarios:
  - name: "Find pets by status"
    flow:
      - log: "Find pets with available status"
      - get:
          url: "/pet/findByStatus?status={{ status }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            - hasProperty: "status"
            - hasHeader: "content-type"
      - think: 1
      - log: "Find pets with pending status"
      - get:
          url: "/pet/findByStatus?status=pending"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            - hasProperty: "status"
            - hasHeader: "content-type"
      - think: 1
      - log: "Find pets with sold status"
      - get:
          url: "/pet/findByStatus?status=sold"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            - hasProperty: "status"
            - hasHeader: "content-type"
      - think: 1

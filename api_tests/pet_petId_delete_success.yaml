# Test generated by RoostGPT for test test_API_SPEC_Postman0711 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=951a0d2a20

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "pet_petId_delete_success.csv"
    fields: ["petId"]
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Delete Pet API test"
    flow:
      - log: "Sending DELETE request to /pet/{petId} endpoint to delete a pet"
      - delete:
          url: "/pet/{{ petId }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "message"
          think: 1

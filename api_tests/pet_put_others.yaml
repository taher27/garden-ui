# Test generated by RoostGPT for test testArtilleryEvent0611 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=a21296b4f6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./functions.js"
  phases:
    - duration: 30
      arrivalRate: 2
  payload:
    path: "pet_put_others.csv"
    fields:
      - "id"
      - "name"
      - "photoUrls"
      - "status"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Pet API failure cases"
    flow:
      - log: "Sending PUT request to /pet with missing required fields"
        put:
          url: "/pet"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            status: "{{ status }}"
        expect:
          - statusCode: 405
          - hasProperty: "message"
          - hasHeader: "content-type"
        think: 1

      - log: "Sending PUT request to /pet with invalid id"
        put:
          url: "/pet"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: 999999999
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            status: "{{ status }}"
        expect:
          - statusCode: 400
          - hasProperty: "message"
          - hasHeader: "content-type"
        think: 1

      - log: "Sending PUT request to /pet with invalid auth"
        put:
          url: "/pet"
          headers:
            Authorization: "InvalidToken"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            status: "{{ status }}"
        expect:
          - statusCode: 401
          - hasProperty: "message"
          - hasHeader: "content-type"
        think: 1

      - log: "Sending PUT request to invalid URL"
        put:
          url: "/invalidUrl"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            status: "{{ status }}"
        expect:
          - statusCode: 404
          - hasProperty: "message"
          - hasHeader: "content-type"
        think: 1

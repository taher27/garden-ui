# Test generated by RoostGPT for test test_API_SPEC_Postman0711 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=831e53a895

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "pet_put_others.csv"
    fields:
      - "name"
      - "photoUrls"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Testing failure cases, edge cases, and error handling for /pet API"
    flow:
      - log: "Testing the scenario where the URL is invalid"
        put:
          url: "/invalid_pet"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
          expect:
            - statusCode: 404
        think: 1
      - log: "Testing the scenario where the required fields are missing in the payload"
        put:
          url: "/pet"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: ""
            photoUrls: ""
          expect:
            - statusCode: 400
            - hasProperty: "message"
        think: 1
      - log: "Testing the scenario where Authorization token is invalid"
        put:
          url: "/pet"
          headers:
            Authorization: "InvalidToken"
          json:
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
        think: 1
      - log: "Testing the scenario where the pet ID is not found"
        put:
          url: "/pet"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: 999999
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
        think: 1
      - log: "Testing the scenario where the pet status is invalid"
        put:
          url: "/pet"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: 10
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            status: "invalid_status"
          expect:
            - statusCode: 405
            - hasProperty: "message"

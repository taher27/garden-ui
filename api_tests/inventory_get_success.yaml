# Test generated by RoostGPT for test testArtilleryEvent0611 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=a66f7079af

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "inventory_get_success.csv"
    fields:
      - "status"
    skipHeader: true

scenarios:
  - name: "Get store inventory by status"
    flow:
      - log: "Making a GET request to /store/inventory to get inventory by status"
        get:
          url: "/store/inventory"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "status"
            - hasHeader: "content-type"
          afterResponse: "think"
      - log: "Expecting a successful operation with a 200 status code and a response body containing an object with integer properties"

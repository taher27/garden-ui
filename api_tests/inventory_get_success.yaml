# Test generated by RoostGPT for test test_API_SPEC_Postman0711 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=80c08bd5c9

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "inventory_get_success.csv"
    fields: ["status", "quantity"]
    skipHeader: true

scenarios:
  - name: "Get inventory status"
    flow:
      - log: "Fetch inventory by status"
      - get:
          url: "/store/inventory"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "status"
            - hasProperty: "quantity"
            # Check the status and quantity are valid
            - equals: 
                - "{{ status }}"
                - "{{ quantity }}"
      - think: 1

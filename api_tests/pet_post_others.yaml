# Test generated by RoostGPT for test test_API_SPEC_Postman0711 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=5bfe8914d4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "pet_post_others.csv"
    fields: ["id", "name", "category_id", "category_name", "photoUrls", "tag_id", "tag_name", "status"]
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Negative testing for /pet API"
    flow:
      - log: "Test case: Missing required field 'name'"
        post:
          url: "/pet"
          json:
            id: "{{ id }}"
            photoUrls: "{{ photoUrls }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
            - hasProperty: "description"
      - think: 1
      - log: "Test case: Missing required field 'photoUrls'"
        post:
          url: "/pet"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
            - hasProperty: "description"
      - think: 1
      - log: "Test case: Invalid authorization token"
        post:
          url: "/pet"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Test case: Invalid URL"
        post:
          url: "/invalidURL"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404

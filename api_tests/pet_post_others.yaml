# Test generated by RoostGPT for test testArtilleryEvent0611 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=16cf30f894

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "pet_post_others.csv"
    fields:
      - "id"
      - "category"
      - "name"
      - "photoUrls"
      - "tags"
      - "status"
    skipHeader: true

scenarios:
  - name: "Testing edge and failure cases for /pet endpoint"
    flow:
      - log: "Sending a POST request with missing required fields in the payload"
        post:
          url: "/pet"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            category: "{{ category }}"
            photoUrls: "{{ photoUrls }}"
            tags: "{{ tags }}"
            status: "{{ status }}"
          expect:
            - statusCode: 405
            - hasProperty: "message"
      - think: 1

      - log: "Sending a POST request with invalid authorization token"
        post:
          url: "/pet"
          headers:
            Authorization: "InvalidToken"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            category: "{{ category }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            tags: "{{ tags }}"
            status: "{{ status }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
      - think: 1

      - log: "Sending a POST request to an invalid URL"
        post:
          url: "/invalidUrl"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            category: "{{ category }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            tags: "{{ tags }}"
            status: "{{ status }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"

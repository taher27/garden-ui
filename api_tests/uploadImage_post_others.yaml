# Test generated by RoostGPT for test testArtilleryEvent0611 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=354b987cbe

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "uploadImage_post_others.csv"
    fields:
      - "petId"
      - "additionalMetadata"
      - "file"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Upload image API test"
    flow:
      - log: "Test Case: Invalid petId"
        post:
          url: "/pet/invalidId/uploadImage"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
        think: 1

      - log: "Test Case: Missing petId"
        post:
          url: "/pet//uploadImage"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
        think: 1

      - log: "Test Case: Invalid Authorization token"
        post:
          url: "/pet/{{ petId }}/uploadImage"
          headers:
            Authorization: "InvalidToken"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
        think: 1

      - log: "Test Case: Missing file"
        post:
          url: "/pet/{{ petId }}/uploadImage"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
        think: 1

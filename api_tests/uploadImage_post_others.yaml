# Test generated by RoostGPT for test test_API_SPEC_Postman0711 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=73f2bf8f3b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "uploadImage_post_others.csv"
    skipHeader: true
    fields:
      - "petId"
      - "additionalMetadata"
      - "imageData"

scenarios:
  - name: "Upload image API negative test cases"
    flow:
      - post:
          url: "/pet/{{ petId }}/uploadImage?additionalMetadata={{ additionalMetadata }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/octet-stream"
          body: "{{ imageData }}"
          log: "Upload Image API Test - Case 1: Valid petId with valid image data but unauthorized access"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - hasHeader: "Content-Type"
          think: 1

      - post:
          url: "/pet/0/uploadImage?additionalMetadata={{ additionalMetadata }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/octet-stream"
          body: "{{ imageData }}"
          log: "Upload Image API Test - Case 2: Invalid petId with valid image data"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - hasHeader: "Content-Type"
          think: 1

      - post:
          url: "/pet/{{ petId }}/uploadImage?additionalMetadata={{ additionalMetadata }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/octet-stream"
          body: ""
          log: "Upload Image API Test - Case 3: Valid petId but empty image data"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - hasHeader: "Content-Type"
          think: 1

      - post:
          url: "/invalidURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/octet-stream"
          body: "{{ imageData }}"
          log: "Upload Image API Test - Case 4: Invalid URL"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - hasHeader: "Content-Type"
          think: 1

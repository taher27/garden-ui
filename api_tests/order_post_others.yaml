# Test generated by RoostGPT for test test_API_SPEC_Postman0711 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=bbe9ff001a

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "order_post_others.csv"
    fields:
      - "id"
      - "petId"
      - "quantity"
      - "shipDate"
      - "status"
      - "complete"
  skipHeader: true
scenarios:
  - name: "Invalid input test for /store/order API"
    flow:
      - think: 5
      - post:
          url: "/store/order"
          headers:
            Authorization: "Bearer InvalidToken"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            petId: "{{ petId }}"
            quantity: "{{ quantity }}"
            shipDate: "{{ shipDate }}"
            status: "{{ status }}"
            complete: "{{ complete }}"
          capture:
            json: "$"
          expect:
            - statusCode: 401
            - hasProperty: "error"
          log: "Testing invalid authorization token"
      - think: 5
      - post:
          url: "/invalid/url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            petId: "{{ petId }}"
            quantity: "{{ quantity }}"
            shipDate: "{{ shipDate }}"
            status: "{{ status }}"
            complete: "{{ complete }}"
          capture:
            json: "$"
          expect:
            - statusCode: 404
          log: "Testing invalid URL"
      - think: 5
      - post:
          url: "/store/order"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "InvalidId"
            petId: "{{ petId }}"
            quantity: "{{ quantity }}"
            shipDate: "{{ shipDate }}"
            status: "{{ status }}"
            complete: "{{ complete }}"
          capture:
            json: "$"
          expect:
            - statusCode: 405
            - hasProperty: "error"
          log: "Testing invalid id in payload"

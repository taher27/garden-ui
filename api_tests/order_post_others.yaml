# Test generated by RoostGPT for test testArtilleryEvent0611 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=036bac41de

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "order_post_others.csv"
    fields:
      - "id"
      - "petId"
      - "quantity"
      - "shipDate"
      - "status"
      - "complete"
    skipHeader: true
  plugins:
    expect: {}
    
scenarios:
  - name: "Test scenario"
    flow:
      - post:
          url: "/store/order"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            petId: "{{ petId }}"
            quantity: "{{ quantity }}"
            shipDate: "{{ shipDate }}"
            status: "{{ status }}"
            complete: "{{ complete }}"
          log: "Sending valid payload to create an order"
          expect:
            - statusCode: 400
            - hasProperty: "message"
        think: 1

      - post:
          url: "/store/order"
          headers:
            authorization: "InvalidToken"
          json:
            id: "{{ id }}"
            petId: "{{ petId }}"
            quantity: "{{ quantity }}"
            shipDate: "{{ shipDate }}"
            status: "{{ status }}"
            complete: "{{ complete }}"
          log: "Sending request with invalid authorization token"
          expect:
            - statusCode: 401
            - hasProperty: "message"
        think: 1

      - post:
          url: "/invalidUrl"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            petId: "{{ petId }}"
            quantity: "{{ quantity }}"
            shipDate: "{{ shipDate }}"
            status: "{{ status }}"
            complete: "{{ complete }}"
          log: "Sending request to invalid URL"
          expect:
            - statusCode: 404
            - hasProperty: "message"
        think: 1

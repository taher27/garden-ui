# Test generated by RoostGPT for test testArtilleryEvent0611 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=6e4c4bbafe

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "order_orderId_delete_success.csv"
    fields:
      - "orderId"
    skipHeader: true

scenarios:
  - name: 'Delete purchase order by ID'
    flow:
      - log: "Starting delete order request"
      - delete:
          url: "/store/order/{{ orderId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: 'Content-Type'
            - hasProperty: 'message'
            - equals: 
                - "{{ message }}"
                - "Order successfully deleted"
        # The above request is expected to delete the order and return a success message
      - think: 1
      - log: "Starting get order request to verify deletion"
      - get:
          url: "/store/order/{{ orderId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: 'Content-Type'
            - hasProperty: 'message'
            - equals: 
                - "{{ message }}"
                - "Order not found"
        # The above request is expected to return 404 as the order has been deleted in the previous request
      - think: 1

# Test generated by RoostGPT for test testArtilleryEvent0611 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=a66f7079af

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "inventory_get_others.csv"
    fields:
      - "status"
    skipHeader: true

scenarios:
  - name: "Checking failure cases, edge cases, and error handling"
    flow:
      - log: "Sending request with invalid authorization token"
        get:
          url: "/store/inventory"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
            - hasHeader: "content-type"
            - equals:
                - "message": "Unauthorized"
      - think: 1

      - log: "Sending request with invalid URL"
        get:
          url: "/store/invalid_inventory"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "content-type"
            - equals:
                - "message": "Not Found"
      - think: 1

      - log: "Sending request without authorization token"
        get:
          url: "/store/inventory"
          expect:
            - statusCode: 403
            - hasHeader: "content-type"
            - equals:
                - "message": "Forbidden"
      - think: 1

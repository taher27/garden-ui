# Test generated by RoostGPT for test testArtilleryEvent0611 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=74f49e1ef7

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "order_orderId_get_others.csv"
    fields:
      - "orderId"
    skipHeader: true

scenarios:
  - name: "Test API endpoint /store/order/{orderId} for failure cases"
    flow:
      - think: 5
      - log: "Request with invalid orderId"
        get:
          url: "/store/order/{{ orderId }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - bodyContains:
                - "Invalid ID supplied"
      - think: 5
      - log: "Request with non-existing orderId"
        get:
          url: "/store/order/non_existing_orderId"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - bodyContains:
                - "Order not found"
      - think: 5
      - log: "Request with invalid URL"
        get:
          url: "/store/invalid_url/{{ orderId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404

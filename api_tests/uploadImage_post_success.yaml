# Test generated by RoostGPT for test test_API_SPEC_Postman0711 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=73f2bf8f3b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "uploadImage_post_success.csv"
    fields:
      - "petId"
      - "additionalMetadata"
      - "file"
    skipHeader: true
  plugins:
    expect: {}
  environments:
    production:
      target: "{{ $processEnvironment.API_HOST }}"
      plugins:
        expect: {}

scenarios:
  - name: "Pet Image Upload Scenario"
    flow:
      - log: "Uploading an image for a pet"
        post:
          url: "/pet/{{ petId }}/uploadImage"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/octet-stream"
          body: "{{ file }}"
          qs:
            additionalMetadata: "{{ additionalMetadata }}"
          expect:
            - statusCode: 200
            - hasProperty: "code"
            - hasProperty: "type"
            - hasProperty: "message"
        think: 1

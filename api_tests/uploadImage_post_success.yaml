# Test generated by RoostGPT for test testArtilleryEvent0611 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=354b987cbe

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "uploadImage_post_success.csv"
    skipHeader: true
    fields:
      - "petId"
      - "additionalMetadata"
      - "file"

scenarios:
  - name: "Upload Image Scenario"
    flow:
      - post:
          url: "/pet/{{ petId }}/uploadImage"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "multipart/form-data"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          beforeRequest: "logUploadImageRequest"
          expect:
            - statusCode: 200
            - hasProperty: "code"
            - hasProperty: "type"
            - hasProperty: "message"
          think: 1


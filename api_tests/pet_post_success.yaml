# Test generated by RoostGPT for test testArtilleryEvent0611 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=16cf30f894

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "pet_post_success.csv"
    fields:
      - "id"
      - "category_id"
      - "category_name"
      - "name"
      - "photoUrls"
      - "tag_id"
      - "tag_name"
      - "status"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Add a new pet to the store"
    flow:
      - post:
          url: "/pet"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            category:
              id: "{{ category_id }}"
              name: "{{ category_name }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            tags:
              - id: "{{ tag_id }}"
                name: "{{ tag_name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "category"
            - hasProperty: "status"
          log: "Post request to add a new pet to the store"
      - think: 1

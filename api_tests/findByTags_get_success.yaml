# Test generated by RoostGPT for test test_API_SPEC_Postman0711 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=9d403edcba

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "findByTags_get_success.csv"
    fields:
      - "tags"
    skipHeader: true
  plugins:
    expect: {}
  processor: "./custom-functions.js"

scenarios:
  - name: "Find pets by tags scenario"
    flow:
      - get:
          url: "/pet/findByTags"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            tags: "{{ tags }}"
          beforeRequest: "logRequest"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "photoUrls"
          think: 1

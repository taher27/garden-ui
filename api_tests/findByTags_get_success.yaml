# Test generated by RoostGPT for test testArtilleryEvent0611 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=c43742dac4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "findByTags_get_success.csv"
    fields:
      - "tags"
    skipHeader: true
  environments:
    variable:
      target: "{{ $processEnvironment.API_HOST }}"

scenarios:
  - name: "Find pets by tags"
    flow:
      - log: "Performing a GET request to find pets by tags"
        get:
          url: "/pet/findByTags"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            tags: "{{ tags }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "category"
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            - hasProperty: "tags"
            - hasProperty: "status"
      - think: 1

# Test generated by RoostGPT for test testArtilleryEvent0611 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=2c3a7f4837

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  defaults:
    headers:
      Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
  payload:
    path: "pet_petId_get_others.csv"
    fields:
      - "petId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Test failure, edge and error handling cases for /pet/{petId} endpoint"
    flow:
      - log: "Test case: Invalid ID supplied"
        get:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 400
            - hasProperty: "message"
        think: 1
      - log: "Test case: Pet not found"
        get:
          url: "/pet/99999999999999999"
          expect:
            - statusCode: 404
            - hasProperty: "message"
        think: 1
      - log: "Test case: Invalid URL"
        get:
          url: "/invalid/pet/{{ petId }}"
          expect:
            - statusCode: 404
        think: 1
      - log: "Test case: Unauthorized access"
        get:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: "message"
        think: 1
